# Environment Configuration Template
# Copy this file and customize for your environment

# ==============================================================================
# API Configuration
# ==============================================================================

# Base API URL (required)
BASE_URL=https://api.example.com

# Specific API endpoints (override if needed)
# API_ENDPOINT=/api/v1/users
# API_VERSION=v1

# ==============================================================================
# Authentication Configuration
# ==============================================================================

# Authentication endpoint (defaults to ${BASE_URL}/auth/token if not set)
AUTH_URL=https://api.example.com/auth/token

# Auth credentials (required)
AUTH_USER=admin
AUTH_PASS=password123

# Token configuration
TOKEN_EXPIRY=600              # Token lifetime in seconds (default: 600 = 10 min)
# TOKEN_PATH=access_token     # JSON path to token in auth response (default: access_token)

# ==============================================================================
# Test Execution Configuration
# ==============================================================================

# Load test parameters
TARGET_TPS=1000               # Target transactions per second
DURATION=5m                   # Test duration (e.g., 30s, 5m, 1h)

# CRUD flow configuration
DELETE_PERCENTAGE=25          # Percentage of iterations that will delete (0-100)

# VU configuration (optional, usually set in test script)
# VUS=10
# ITERATIONS=100

# ==============================================================================
# Error Handling Configuration
# ==============================================================================

# Error logging
# ERROR_LOG_FILE=errors.log   # Optional: Write errors to file

# Threshold configuration (set in test script, but can override here)
# MAX_ERROR_RATE=0.20         # Max business error rate before abort (default: 0.20 = 20%)
# MAX_RESPONSE_TIME=5000      # Max p95 response time in ms

# ==============================================================================
# Data Configuration
# ==============================================================================

# CSV file paths (optional, defaults to ./data/)
# USER_DATA_FILE=./data/users.csv
# AUTH_DATA_FILE=./data/auth.csv
# ORDER_DATA_FILE=./data/orders.csv

# ==============================================================================
# Environment-Specific Settings
# ==============================================================================

# Development
# BASE_URL=https://dev-api.example.com
# AUTH_USER=dev-user
# AUTH_PASS=dev-password

# Staging
# BASE_URL=https://staging-api.example.com
# AUTH_USER=staging-user
# AUTH_PASS=staging-password

# Production (use with caution!)
# BASE_URL=https://api.example.com
# AUTH_USER=prod-test-user
# AUTH_PASS=prod-test-password
# TARGET_TPS=100              # Lower TPS for production testing

# ==============================================================================
# Usage Examples
# ==============================================================================

# Load from file:
# k6 run --env-file=./config/dev.env tests/script1-full-crud.js

# Override individual values:
# k6 run \
#   -e BASE_URL=https://api.example.com \
#   -e AUTH_USER=admin \
#   -e TARGET_TPS=500 \
#   tests/script2-high-tps.js

# ==============================================================================
# Notes
# ==============================================================================

# 1. Environment variables take precedence over defaults in scripts
# 2. Use separate config files for different environments (dev, staging, prod)
# 3. Never commit production credentials to version control
# 4. Use k6 cloud or secrets management for sensitive data in CI/CD
